#ifndef PBL_DEVICES_RELAY_HPP__
#define PBL_DEVICES_RELAY_HPP__

namespace pbl::devices
{

// Relay Controller that uses MC23017 behind the scenes to control relays
class Relay
{
public:
	// template < typename T >
	// using Result = utils::Result< T >;

	// Result on(std::size_t pinIdx) {}
	// Result off(std::size_t pinIdx) {}
	// Result toogle(std::size_t pinIdx) {}
};

} // namespace pbl::devices
#endif // PBL_DEVICES_RELAY_HPP__
